# Experimental data

This directory stores generated test inputs and CSV timing results.

- `inputs/`: reproducible insertion and deletion sequences.
- `results.csv`: consolidated benchmark observations (generated later).

